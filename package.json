{
  "name": "dsh-chat-tick-rail",
  "version": "0.3.3",
  "type": "module",
  "description": "dsh web UI plugin: in-session message navigation rail (one tick per user message, viewport marker, click to jump)",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "node --test \"test/*.test.cjs\"",
    "prepublishOnly": "npm test",
    "release": "npm test && npm version patch -m \"chore(release): bump version to %s\" && npm publish && git push origin main --follow-tags"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "license": "MIT"
}
